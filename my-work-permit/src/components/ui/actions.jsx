import React from 'react';

export const Refresh = ({ onClick }) => <button onClick={onClick}>Refresh</button>;
export const Withdraw = ({ onClick }) => <button onClick={onClick}>Withdraw</button>;
export const Delete = ({ onClick }) => <button onClick={onClick}>Delete</button>;
export const PreventEmail = ({ onClick }) => <button onClick={onClick}>Prevent Email</button>;
